{
  "buildCommand": "CI=false npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "build",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
